# 本次修改说明

本轮主要围绕结构整理、角色表现、音频链路和界面稳定性做了集中修复。

项目结构方面，已将原本过于臃肿的主流程拆分到 `core`、`services`、`ui`、`utils` 等目录，便于后续继续维护。角色相关增加了独立 skill / prompt 管理，回复解析、资源路径、图片处理、线程池等公共逻辑也做了归类。

功能方面，修复并增强了多项体验问题：包括图片上传异常、情绪标签解析、预设音频同步日文字幕、永久记忆保存与读取、聊天输入框偶发锁死、调试窗口、启动动画等待完整播放、设置窗口闪烁/错位、模型列表可读性等。角色立绘显示也重新整理，恢复透明背景逻辑，避免人物边缘被底色遮挡。

音频方面，加入了 Vocu 专用网络流播放器，支持 `simple-generate`、异步生成、Flash 开关、失败回退到缓存/Qt 播放，并补充了相应设置项与说明。与此同时，也针对启动白屏一帧、调试窗口自动滚到底等细节做了修正。

整体上，这次修改的目标不是单点补丁，而是把项目从“能跑”推进到“更稳、更清晰、后续更容易继续改”。如需，我下一步可以再补一份“当前项目结构说明”供你以后自己找文件。
